# Python dependencies for ESP32 examples
# This file is used by CI caching to determine when to invalidate Python dependency cache

# Core dependencies
pyyaml>=6.0
click>=8.0
colorama>=0.4.0

# ESP-IDF related dependencies (these are typically installed by ESP-IDF installer)
# but we list them here for cache invalidation purposes
esptool>=4.0
esp-idf-kconfig>=1.0
esp-idf-size>=1.0
